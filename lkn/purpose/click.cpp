/** purpose click **/

#include "stdio.h"
#include "stdlib.h"
#include "stdarg.h"
#include "stdint.h"
#include "string"

#if 0
namespace PurposeClick {

class Click {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeEvent {

class ClickEvent {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeClass {

class ClickClass {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposePromise {

class ClickPromise {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}


namespace PurposeObject {

class ClickObject {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeInput {

class ClickInput {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeDialog {

class ClickDialog {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeKits {

class ClickKits {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeCeck {

class ClickCheck {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeUtils {

class ClickUtils {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeGit {

class ClickGit {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeUrl {

class ClickClassUrl {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeHits {

class ClickHits {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}


namespace PurposeLocal {

class ClickLocal {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeTypes {

class ClickTypes {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeDialog {

class ClickDialog {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeChanges {

class ClickChanges {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}

namespace PurposeBase {

class ClickCheckBase {

      public static void clickAudioReference(const char* cin, char* cin){

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      };

      void clickAttackReference(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLossBody(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }

      void clickLapLoop(const char* cin, char* cin) {

             int a, b, sum;

             cin >> a;
             cin >> b;

             sum = a + b;
             cout << sum << endl;


      }
};

}
#endif // 0
